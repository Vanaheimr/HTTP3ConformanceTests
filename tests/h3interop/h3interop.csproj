<Project Sdk="Microsoft.NET.Sdk">

  <!-- Unlike the other two harnesses this one IS our client: the whole point is to point our own
       stack at foreign servers. h3semantics/h3attack test the opposite direction and deliberately
       borrow nothing from us. -->

  <ItemGroup>
    <ProjectReference Include="..\..\libs\Hermod\Hermod\Hermod.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
