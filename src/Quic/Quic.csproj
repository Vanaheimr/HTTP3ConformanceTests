<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>org.GraphDefined.Vanaheimr.Hermod.Quic</RootNamespace>
  </PropertyGroup>

  <!-- TargetFramework, Nullable, ImplicitUsings etc. come from Directory.Build.props -->

  <ItemGroup>
    <!-- Quic.Core comes in transitively via Quic.Tls (deliberate, no direct reference). -->
    <ProjectReference Include="..\Quic.Tls\Quic.Tls.csproj" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Http3.Tests" />
  </ItemGroup>

</Project>
